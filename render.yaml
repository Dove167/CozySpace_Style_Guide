services:
  - type: web
    name: cozyspace-style-guide
    runtime: docker
    plan: free
    buildCommand: bun install && turbo build
    startCommand: cd server && bun run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /api/health