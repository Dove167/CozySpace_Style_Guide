{
  "name": "server",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "bun --watch run src/index.ts",
    "build": "echo 'Skipping server build - Bun runs TypeScript directly'",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "hono": "^4.7.11",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
